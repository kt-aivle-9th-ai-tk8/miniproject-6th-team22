apiVersion: v1
kind: Service
metadata:
  name: backend-service      
  namespace: default
spec:
  type: ClusterIP                   
  selector:
    app: team22-backend             
  ports:
  - port: 80                      
    targetPort: 8080                