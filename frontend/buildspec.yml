version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20

  pre_build:
    commands:
      - cd frontend
      - npm ci

  build:
    commands:
      - npm run build
      - find . -name index.html

artifacts:
  base-directory: frontend/dist
  files:
    - '**/*'